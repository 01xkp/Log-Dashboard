# 仅填写日志服务根地址；前端会固定拼接 /api/v1/diagnostic-logs。
# 生产和非本机环境必须使用 HTTPS，例如：https://logs.example.internal
# 仅开发环境允许：http://localhost:8080 或 http://127.0.0.1:8080
# 留空时，看板只启用本地多日志导入模式，不会发起网络请求。
VITE_LOG_API_BASE_URL=
